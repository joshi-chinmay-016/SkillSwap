apiVersion: 1

providers:
  - name: "SkillSwap Dashboards"
    orgId: 1
    folder: "SkillSwap"
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
